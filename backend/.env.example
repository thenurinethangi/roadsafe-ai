# Optional. The backend works without this file, using the same values below.
# Copy to backend/.env only if your database runs somewhere else.
DATABASE_URL=postgresql://roadsafe:roadsafe@127.0.0.1:5433/roadsafe

OSRM_BASE_URL=https://router.project-osrm.org
WEATHER_BASE_URL=https://api.open-meteo.com/v1/forecast
MODEL_DIR=../ml/artifacts
